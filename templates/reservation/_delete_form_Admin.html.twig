<form method="post" action="{{ path('app_reservation_delete_Admin', {'id': reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
    
    <button class="btn btn-block  btn-sm" style="padding: 5px; margin-left: 0px; border-radius: 8px; border: none; cursor: pointer;color:rgb(245, 172, 0)">
    <i class="fas fa-trash-alt"></i>
</button>

</form>
